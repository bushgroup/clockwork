"""The PySide6 window: one pane per box, Send all, Acquire, Replicate.

The only package in `clockwork` that imports Qt. Filled by the lab record's
task 08.
"""

from __future__ import annotations


def main() -> int:
    """Console-script entry point. The window does not exist yet."""
    raise SystemExit("clockwork: the application window is not built yet (lab record, task 08)")
